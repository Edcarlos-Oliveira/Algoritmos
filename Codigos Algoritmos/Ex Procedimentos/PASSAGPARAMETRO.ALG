algoritmo "PassagParametro"
var
   X, Y: Inteiro
Procedimento Soma (A, B: Inteiro)//Para somar os valores de X e Y como A e B
inicio
      Escreval("Recebi o valor ", A)
      Escreval("Recebi o valor ", B)
      Escreval("A soma entre os dois é ", A + B)
FimProcedimento
inicio
      X <- 5
      Y <- 3
      Soma (X, Y) //como não existe soma, cria o procedimento
fimalgoritmo